ScopedBookmarkAgent(8)       System Manager's Manual      ScopedBookmarkAgent(8)

NAME
     ScopedBookmarkAgent

DESCRIPTION
     ScopedBookmarkAgent provides agent services for creating and resolving
     security scoped bookmarks. It is not meant to be invoked directly.

