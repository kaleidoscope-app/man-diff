otherbsd(8)                  System Manager's Manual                 otherbsd(8)

NAME
     otherbsd – Other Bootstrapper Daemon

DESCRIPTION
     otherbsd is responsible for bootstrapping launchd(8) jobs that launchd(8)
     cannot find directly for layering reasons, including agents in a user's
     home directory and login items.

