NetAuthAgent(8)              System Manager's Manual             NetAuthAgent(8)

NAME
     NetAuthAgent – Shows dialogs on behalf of NetAuthSysAgent.

DESCRIPTION
     NetAuthAgent displays dialogs to the user so they can authenticate with a
     network file server and select shares to mount.

