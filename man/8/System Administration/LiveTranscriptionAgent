LiveTranscriptionAgent(8)    System Manager's Manual   LiveTranscriptionAgent(8)

NAME
     LiveTranscriptionAgent – Live Transcription Launch Agent

DESCRIPTION
     Captures user's audio and renders as text via audio input

