logd_reporter(8)             System Manager's Manual            logd_reporter(8)

NAME
     logd_reporter – unified logging system reporter daemon

DESCRIPTION
     logd_reporter is a daemon that periodically observes the behavior of
     logd(8).  It should not be directly invoked; it is only intended for system
     use.

SEE ALSO
     logd(8)

