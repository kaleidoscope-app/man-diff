system_installd(8)           System Manager's Manual          system_installd(8)

NAME
     system_installd – macOS software installation daemon

DESCRIPTION
     system_installd is used by the system during package installation. It
     should not be invoked manually.

SEE ALSO
     installer(8), pkgutil(1), installd(8),

