amfid(8)                     System Manager's Manual                    amfid(8)

NAME
     amfid – The mobile file integrity daemon.

DESCRIPTION
     amfid amfid is a daemon that checks the integrity of files running on the
     system.  It is not intended to be invoked directly.

SEE ALSO
     launchd(8) launchd.conf(5)

