LIVEFILEPRVIDERD(8)          System Manager's Manual         LIVEFILEPRVIDERD(8)

NAME
     livefileproviderd – LIFS mount manager and mount support

DESCRIPTION
     livefileproviderd is the LIFS mount management and mount support daemon. It
     manages the LIFS mount life cycle and coordinating with other daemons. It
     also translates requests from the kernel LIFS file system to the user space
     file system servers backing volumes.

     livefileproviderd is invoked invoked by launchd(8) when volume creation is
     requested or when previously-created volumes are detected.

FILES
     /System/Library/LaunchAgents/com.apple.livefileproviderd.plist

