calaccessd(8)                System Manager's Manual               calaccessd(8)

NAME
     calaccessd – calendar data process

DESCRIPTION
     calaccessd is the calendar data process. It is used by the system to
     coordinate access to calendar data and to post calendar notifications.

     There are no configuration options to calaccessd, and users should not run
     calaccessd manually.

