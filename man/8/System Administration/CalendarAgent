CalendarAgent(8)             System Manager's Manual            CalendarAgent(8)

NAME
     CalendarAgent – calendar data process

DESCRIPTION
     CalendarAgent is the calendar data process.  It is used by the system to
     coordinate access to calendar and reminder data.

     There are no configuration options to CalendarAgent, and users should not
     run CalendarAgent manually.


Mac OS X                        January 17, 2017                        Mac OS X
